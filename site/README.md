# wrld sneakers — премиум магазин кроссовок 👟

Современный статический сайт для каталога эксклюзивных кроссовок от ведущих мировых брендов.

## 🚀 Быстрый запуск

Для локального просмотра запустите HTTP-сервер в папке `site/`:

```bash
cd site
python3 -m http.server 8000
```

Затем откройте в браузере: **http://localhost:8000**

### Альтернативные способы запуска:

**Node.js (если установлен):**
```bash
npx http-server site -p 8000
```

**PHP:**
```bash
cd site
php -S localhost:8000
```

## 📁 Структура проекта

```
site/
├── index.html       # Главная страница
├── styles.css       # Современный дизайн с градиентами и анимациями
├── app.js           # JavaScript логика (загрузка данных, рендеринг)
├── data.json        # Метаданные всех брендов и моделей
├── assets/          # Копии всех изображений
│   ├── adidas/
│   ├── bape/
│   ├── converse/
│   ├── crocs/
│   ├── icon/        # Логотипы всех брендов + иконка магазина
│   ├── new_balance/
│   ├── nike/
│   ├── onitsuka_tiger/
│   └── puma/
└── README.md        # Этот файл
```

## ✨ Возможности

- 🎨 **Современный дизайн** с градиентами, анимациями и плавными переходами
- 🏷️ **Логотипы брендов** — все 8 брендов имеют оригинальные иконки
- 📱 **Адаптивная вёрстка** (работает на десктопе, планшете и мобильном)
- 🔍 **Автоматическая сортировка** брендов и моделей в алфавитном порядке
- 🖼️ **Заглушки изображений** при отсутствии фото
- ⚡ **Быстрая загрузка** и плавная анимация карточек
- 💰 **Отображение цен** (оценочные цены помечены знаком ≈)
- 📏 **Список размеров** для каждой модели

## 🎯 Как это работает

1. При загрузке страницы JavaScript загружает `data.json`
2. Бренды отображаются в левой панели с иконками (если есть)
3. При клике на бренд показываются все его модели
4. Все карточки анимированы с задержкой для плавного появления
5. При наведении на карточку — масштабирование изображения и подъём карточки

## 🛠️ Технологии

- **HTML5** — семантическая разметка
- **CSS3** — современный дизайн с переменными, градиентами, анимациями
- **Vanilla JavaScript** — без фреймворков, чистый JS
- **Google Fonts** — шрифт Inter для лучшей типографики

## 📝 Примечания

- Все изображения скопированы в `site/assets/` для корректной работы
- Цены являются **оценочными** (поле `price_estimated: true` в JSON)
- Если изображение не загружается — показывается заглушка «Нет фото»
- Первый бренд автоматически выбирается при загрузке страницы

## 🎨 Кастомизация

Вы можете легко изменить цветовую схему в `styles.css`:

```css
:root {
  --bg-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --primary: #667eea;
  --secondary: #f56565;
  --success: #48bb78;
}
```

## 📊 Данные

Структура `data.json`:
```json
{
  "brands": [
    {
      "name": "nike",
      "brand_icon": "assets/nike/icon.png" | null,
      "models": [
        {
          "name": "Air Force 1",
          "image": "assets/nike/Air_Force_1.png" | null,
          "price": 110,
          "currency": "USD",
          "price_estimated": true,
          "sizes": [36, 37, 38, ...],
          "description": "..."
        }
      ]
    }
  ]
}
```

---

Создано с ❤️ для **wrld sneakers**

## 📦 Деплой на GitHub Pages

Два простых варианта разместить сайт на GitHub Pages:

1) Использовать автоматический workflow (рекомендуется)

- При пуше в ветку `main` GitHub Actions запустит workflow `.github/workflows/gh-pages.yml`, которое возьмёт содержимое папки `site/` и опубликует его в ветке `gh-pages`.
- Включать Pages в настройках репозитория не обязательно — после первого успешного деплоя страница будет доступна по URL: `https://<username>.github.io/<repo>/`.

2) Ручной вариант — поместить содержимое `site/` в папку `docs/` в ветке `main` и включить GitHub Pages в настройках репозитория, выбрав источник `main / docs`.

Дополнительно:
- В папке `site/` есть файл `.nojekyll`, чтобы GitHub Pages не пытался обрабатывать сайт через Jekyll.
- Если вы хотите использовать кастомный домен — добавьте файл `CNAME` в `site/` с доменом и настройте DNS.

Если нужно, могу: автоматически создать pull request, который добавит workflow и включит Pages (через инструкцию), или настроить имя CNAME и обновить README с точным URL после деплоя.
